server:
  port: 8888
spring:
  application:
    name: course


  datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
  open-in-view: false
logging:
    level:
      root:
      #com.udemyCourse.course: WARN
spring-doc:
    paths-to-match: /person
    swagger-ui:
      use-root-path: true
