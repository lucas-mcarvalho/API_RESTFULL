server:
  port: 8888

spring:
  application:
    name: course

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
security:
  jwt:
    token:
      secret-key: 57cr37
      expire-length: 3600000
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
  open-in-view: false

logging:
  level:
    root:

springdoc:
  paths-to-match:
  swagger-ui:
    use-root-path: true
